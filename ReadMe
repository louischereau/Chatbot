1. To run the front end : node s3-upload.js

2. To run the backend server:
   a. source venv/bin/activate
   b. cd chatbot
   c. pip install -r requirements.txt
   d. export AWS_ACCESS_KEY=...
   e. export AWS_SECRET_ACCESS_KEY=...
   f. rasa run -m models --enable-api --cors "*" --debug
   g. rasa run actions --cors "*"
